on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u erwin bash -c 'cat > /home/erwin/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/erwin/ansible/inventory.yaml
          roles_path    = /home/erwin/ansible/roles
          host_key_checking = False
          remote_user = erwin
          private_key_file = /home/erwin/.ssh/id_rsa
          log_path = /home/erwin/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/erwin/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/erwin/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown erwin:erwin /home/erwin/ansible/ansible.cfg
          sudo chmod 644 /home/erwin/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u erwin ls -la /home/erwin/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u erwin cat /home/erwin/ansible/ansible.cfg
